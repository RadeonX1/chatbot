import re
def swap_pronouns(phrase):

    if 'I' in phrase:

        return re.sub('I', 'You', phrase)

    if 'my' in phrase:

        return re.sub('my', 'your', phrase)

    else:

        return phrase

print(swap_pronouns("my walk to school"))